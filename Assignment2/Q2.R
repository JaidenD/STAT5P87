  # IN: int sample_size or vector outputs, folds, stratified (optional), seed (optional) 
  kfold <- function(sample_size = 0, outputs = vector(), folds, stratified = FALSE, seed = 0){
    
  }
  
  
  
